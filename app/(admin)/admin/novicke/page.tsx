import { getAllSubscribers } from "@/lib/subscriberActions";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Novičke",
};

async function Page() {
  return <div></div>;
}

export default Page;
